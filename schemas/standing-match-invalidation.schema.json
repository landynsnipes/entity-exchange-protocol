{
  "$ref": "#/definitions/standing-match-invalidation",
  "definitions": {
    "standing-match-invalidation": {
      "type": "object",
      "properties": {
        "standingVersion": {
          "type": "string",
          "const": "0.1.0-draft.1"
        },
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "notificationId": {
          "type": "string",
          "format": "uuid"
        },
        "proposalId": {
          "type": "string",
          "format": "uuid"
        },
        "reason": {
          "type": "string",
          "enum": [
            "source_state_changed",
            "view_withdrawn",
            "intent_expired",
            "authorization_revoked",
            "consent_revoked"
          ]
        },
        "invalidatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "standingVersion",
        "id",
        "notificationId",
        "proposalId",
        "reason",
        "invalidatedAt"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
